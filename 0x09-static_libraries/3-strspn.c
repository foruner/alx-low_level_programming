#include"main.h"
/**
 * _strspn - clculate initial segment length of a string
 * @s:pointer to the string to be examined
 * @accept:pointer to a string containing character to be searched
 * Return: Always 0 (Success)
 */
unsigned int _strspn(char *s, char *accept)
{
}
